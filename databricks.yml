# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "test"

sync:
  exclude:
    - src/*.lvdash.json

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com
      root_path: /Users/bruno.tafur@databricks.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: bruno.tafur@databricks.com
